/*
import * as PIXI from 'pixi.js';
import { log } from '../utils/utils.js';

export function grSetupScene(app) {
    log('grSetupScene called');

    // Добавление текста (пример)
    const text = new PIXI.Text('Hello from Graphics!', {
        fontFamily: 'Arial',
        fontSize: 24,
        fill: 0xffffff,
    });
    text.anchor.set(0.5);
    text.x = app.renderer.width / 2;
    text.y = 50;
    app.stage.addChild(text);

    return {
        update: (delta) => {
            // Можете добавить сюда логику обновления для графических элементов,
            // не связанных с физикой, если это необходимо.
        },
    };
} 
*/